<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vizualis">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".clicker.ClickerActivity"></activity>
        <activity android:name=".clicker_mvvm.ClickerActivity"></activity>
        <activity android:name=".fragment.SimpleFragmentActivity" />
        <activity android:name=".AlertsTabsPagesActivity" />
        <activity android:name=".shopping.DatabaseSampleActivity" />
        <activity android:name=".CardViewActivity" />
        <activity android:name=".StylesThemesActivity" />
        <activity android:name=".IncludeActivity" />
        <activity android:name=".recycleviewscollview.RecycleViewActivity" />
        <activity android:name=".GridExampleActivity" />
        <activity android:name=".recycleviewscollview.ScrollViewActivity" />
        <activity android:name=".chat.ChatSecondActivity" />
        <activity android:name=".chat.ChatFirstActivity" />
        <activity android:name=".ListExampleActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>